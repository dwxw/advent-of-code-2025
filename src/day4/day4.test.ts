import { expect } from "@std/expect";
import { day4, day4Part2 } from "./day4.ts";

const test = Deno.test;

test("example", () => {
  expect(day4([])).toBe(0);
});

test("example", () => {
  expect(day4Part2([])).toBe(0);
});
